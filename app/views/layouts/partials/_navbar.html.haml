%nav.navbar.is-spaced.has-shadow
  .container
    .navbar-brand
      = link_to root_url(subdomain: request.subdomain), class: "navbar-item" do
        %h6.title.is-5
          Doctor X
          %sup
            %i.fas.fa-2x.fa-user-md
      %span.navbar-burger.burger{ "data-target" => "navMenu", :role => "button"}
        %span{"aria-hidden" => "true"}
        %span{"aria-hidden" => "true"}
        %span{"aria-hidden" => "true"}
    #navMenu.navbar-menu
      .navbar-end
        - if user_signed_in?
          .navbar-item.has-dropdown.is-hoverable
            .navbar-link
              = current_user.name
            .navbar-dropdown
              .navbar-item
                %a
                  %i.fas.fa-user-circle
                  Perfil
              .navbar-item
                = link_to destroy_user_session_path, method: :delete do
                  %i.fas.fa-sign-out-alt
                  Cerrar Sesión

