= render "shared/section" do
  %div.flex.flex-col.md:flex-row.md:justify-between
    = render "shared/actions", model: :referred_doctor, search_path: referred_doctors_path

= render "shared/section" do
  = render "shared/table" do
    %thead.border-b.border-gray-300.dark:border-gray-700.text-gray-400
      %tr
        %th.py-2{scope: :col} Nombre Completo
        %th.py-2{scope: :col} Especialidad
        %th.py-2{scope: :col} Número Teléfonico
        %th.py-2{scope: :col} Acciones
    %tbody
      - @referred_doctors.each do |referred_doctor|
        %tr.cursor-pointer.hover:bg-gray-200.dark:hover:bg-gray-700
          %td.whitespace-nowrap.py-4= referred_doctor.full_name
          %td.whitespace-nowrap.py-4= referred_doctor.specialty
          %td.whitespace-nowrap.py-4= call_to referred_doctor.phone_number
          %td
            = link_to :editar, [:edit, referred_doctor]
            = link_to :detalle, referred_doctor
